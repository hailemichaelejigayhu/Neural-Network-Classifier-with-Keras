# Neural-Network-Classifier-with-Keras
A deep learning project demonstrating image classification using Keras with MNIST and Fashion-MNIST datasets. Includes feedforward and convolutional neural networks (CNNs) trained and evaluated on different hardware setups.
